# Install Resume Tailor Dependencies

Read `docs/guide/installation.md` and `install/agent-install.yaml`. Install `requirements.txt`, run `scripts/check_agent_platform_support.py`, and report any baseline failure. Do not download or clone dependency Skills.
