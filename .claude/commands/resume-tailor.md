# Resume Tailor Workflow

Read `SKILL.md`, resolve the Skill directory and the user's separate resume workspace, then follow the full workflow. Extract supported source files with `scripts/extract_resume_text.py`, preserve factual boundaries, generate through the transactional PDF command, and report the absolute accepted PDF path.
